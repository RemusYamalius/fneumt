

# خطة: تحسين شامل للفلتر الدائري والخارطة التفاعلية

## التغييرات المطلوبة

### 1. `src/components/OrbitalFilter.tsx` — إعادة هيكلة التخطيط

**التخطيط الجديد (من الأعلى للأسفل):**
```text
┌────────────────────────────────┐
│   عجلة الألوان (كبيرة جداً)     │
│   تأخذ كامل العرض المتاح       │
│   viewBox أكبر + max-w أكبر    │
└────────────────────────────────┘
┌────────────────────────────────┐
│  منطقة التحديدات التدريجية     │
│  (تظهر badges ملونة لكل       │
│   عنصر تم تحديده في الحلقات)   │
└────────────────────────────────┘
┌────────────────────────────────┐
│  فلاتر إضافية (مؤسسة/عمر/     │
│  PPR/هاتف) مع ألوان وظلال     │
│  + أزرار بحث/ضبط              │
└────────────────────────────────┘
```

- تغيير التخطيط من `flex-row` إلى `flex-col`
- تكبير العجلة: زيادة `max-w` من `420px` إلى `520px`، وزيادة أنصاف أقطار الحلقات
- زيادة حجم الخط داخل الشرائح ليصبح مقروءاً
- إضافة منطقة وسطى بين العجلة والفلاتر الإضافية تعرض التحديدات بشكل تدريجي (كل ما يُنقر عنصر يظهر badge ملون مع AnimatePresence)
- تجميل الفلاتر الإضافية: خلفيات ملونة متدرجة، ظلال، حدود مستديرة، أيقونات ملونة

### 2. `src/components/MoroccoMap.tsx` — تحسينات

**عرض اسم الإقليم بجانب الجهة:**
- عند النقر على إقليم، يظهر اسمه بجانب اسم الجهة في شريط الرجوع (مثلاً: "جهة X › إقليم Y")

**إضافة مفتاح إحصائي على جوانب الخارطة (مستوحى من الصورة المرفقة):**
- لوحة يسرى علوية: إجمالي المسجلين + رسم بياني صغير (sparkline)
- لوحة يسرى سفلية: إحصائيات تفصيلية (منخرطون، غير منخرطين، طلبات)
- لوحة يمنى: توزيع حسب الحالة (نسب مئوية مع دوائر صغيرة)
- كل لوحة بخلفية `#001D39` شبه شفافة مع حدود `#49769F` وظلال
- تظهر بـ AnimatePresence عند اختيار جهة/إقليم

### 3. `src/pages/QuickFilter.tsx` — تعديل طفيف
- تمرير `selectedProvinceName` جديد من MoroccoMap لعرضه في شريط الرجوع
- تمرير `regionStats` للإحصائيات الجانبية

## الملفات المتأثرة
```text
src/components/OrbitalFilter.tsx  (إعادة هيكلة التخطيط + تكبير + تحديدات تدريجية + تجميل)
src/components/MoroccoMap.tsx     (اسم الإقليم + لوحات إحصائية جانبية)
src/pages/QuickFilter.tsx        (تمرير props جديدة)
```


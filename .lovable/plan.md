

## التعديلات المطلوبة على الشريط الفرعي (Sub-bar) في `src/pages/Dashboard.tsx`

### 1. تكبير حجم الخط
تغيير `text-xs` إلى `text-sm` في سطر 288 (الضعف تقريباً).

### 2. تأثير البريق اللامع (Shimmer)
- إضافة `overflow-hidden relative` على الشريط الفرعي (سطر 286)
- إضافة عنصر `::after` pseudo-element أو `div` مطلق بتدرج شفاف أبيض يمر أفقياً
- الاتجاه: RTL → يمين لليسار (عربي)، LTR → يسار لليمين (فرنسي) — يُحدد بـ `dir`
- التكرار: كل 5 ثوانٍ، مدة المرور ~1.5 ثانية

### 3. نبض الأحرف عند مرور البريق
- تقسيم `roleLabel` إلى أحرف فردية داخل `<span>` لكل حرف
- استخدام `framer-motion` لإضافة `animate` بتأخير متتابع (stagger) يتزامن مع مرور البريق
- كل حرف يحصل على `scale` طفيف (1 → 1.15 → 1) عند وصول البريق إليه

### التنفيذ في `src/pages/Dashboard.tsx` و `src/index.css`

**index.css**: إضافة keyframe `shimmer-rtl` و `shimmer-ltr` للبريق.

**Dashboard.tsx** (سطر 285-289):
- تحويل النص من `<p>` بسيط إلى مكون يقسم الأحرف
- استخدام `useEffect` + `useState` لتشغيل دورة النبض كل 5 ثوانٍ
- كل حرف يحصل على `motion.span` مع تأخير حسب موقعه


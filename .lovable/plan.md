

# خطة: تطبيق لوحة الألوان الجديدة على خلفية الخارطة وعناصرها

## التغييرات

### لوحة الألوان المرجعية (من الصورة)
```text
#001D39  — أزرق داكن جداً (للنصوص/العناصر الأغمق)
#0A4174  — أزرق ملكي داكن
#49769F  — أزرق متوسط
#4E8EA2  — أزرق تيل
#6EA2B3  — أزرق فاتح-تيل
#7BBDE8  — أزرق سماوي
#BDD8E9  — أزرق فاتح جداً (خلفية الخارطة)
```

### 1. `src/pages/QuickFilter.tsx`
- تغيير خلفية حاوية الخارطة (السطر 181) من `from-[hsl(225,40%,8%)] to-[hsl(225,50%,12%)]` إلى `bg-[#BDD8E9]` (اللون الفاتح المطلوب)
- إزالة أو تعديل عناصر الـ glow الزخرفية لتتناسب مع الخلفية الفاتحة

### 2. `src/components/MoroccoMap.tsx`
- **زر الرجوع** (السطر 241): تغيير من `bg-accent/30` إلى `bg-[#0A4174] text-white hover:bg-[#001D39]`
- **اسم الجهة** (السطر 246): تغيير `text-foreground` إلى `text-[#001D39] font-bold`
- **خلفية الجهة في وضع الغوص** (السطر 284): من `hsl(225, 40%, 15%)` إلى `#BDD8E9`
- **حدود الجهة** (السطر 285): من `hsl(225, 60%, 40%)` إلى `#0A4174`
- **ألوان التمرير/التحديد في الأقاليم**: استخدام `#0A4174` للتحديد و `#49769F` للتمرير

### 3. `src/lib/morocco-regions.ts`
- تحديث مصفوفة `REGION_COLORS` لاستخدام الألوان الجديدة:
  - `#001D39`, `#0A4174`, `#49769F`, `#4E8EA2`, `#6EA2B3`, `#7BBDE8` مع تكرار/تنويع

### الملفات المعدلة
```text
src/pages/QuickFilter.tsx        (خلفية الحاوية)
src/components/MoroccoMap.tsx     (زر الرجوع + اسم الجهة + ألوان SVG)
src/lib/morocco-regions.ts       (لوحة الألوان)
```


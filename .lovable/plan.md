

# تحسين الشريط العلوي للوحة التحكم

## التغييرات المطلوبة في `src/pages/Dashboard.tsx` (سطر 232-261)

### 1. نقل وصف المنصب إلى شريط فرعي أسفل الشريط الأزرق
- إضافة `div` جديد أسفل الـ `header` بخلفية شبه شفافة (مثلاً `bg-primary/5` أو `bg-white/5`) يعرض `roleLabel` بلون مميز (مثلاً أبيض/70 على خلفية داكنة أو primary على خلفية فاتحة)
- هذا يوفر مساحة في الشريط الرئيسي

### 2. تكبير البادج ووضعه بجانب أيقونة اللغة
- نقل `VerifiedBadge` من بجانب النص إلى بجانب أيقونة Globe في مجموعة الأزرار اليمنى
- تعيين حجم البادج بنفس حجم الأيقونات (`w-5 h-5` = `size={20}`)

### 3. إظهار اللوغو بشكل أكبر على الموبايل
- تغيير حجم اللوغو من `w-20 h-20` (مخفي/صغير على الموبايل) إلى حجم مناسب مع responsive: `w-12 h-12 sm:w-16 sm:h-16`
- إزالة أي `hidden` على الموبايل

### الهيكل الجديد:
```text
┌─────────────────────────────────────────┐
│ [Logo]  FNE-UMT    [Badge][🌐][🔔][🚪] │  ← الشريط الأزرق الرئيسي
├─────────────────────────────────────────┤
│  نائب(ة) منسق(ة) — الأكاديمية / ...   │  ← شريط فرعي بلون مميز
└─────────────────────────────────────────┘
```

### ملف واحد: `src/pages/Dashboard.tsx`


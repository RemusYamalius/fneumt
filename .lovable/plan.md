

# خطة: تحسين بطاقة العد التنازلي

## المطلوب
1. حذف الأرقام الكبيرة المائلة خارج البطاقة (السطور 61-75) التي تحجب رؤية محتوى الصفحة
2. إزالة الخلفية المعتمة الداكنة (`background: hsla(220 30% 8% / 0.85)`) واستبدالها بخلفية شفافة تماماً مع الإبقاء على `pointer-events` للحماية
3. تكبير اللوغو إلى الضعف (`w-40 h-40`) وجعل نصفه العلوي يبرز خارج البطاقة من أعلى (floating style كما في صفحة تسجيل الدخول) باستخدام `margin-top` سالب وإزالة `overflow-hidden` من البطاقة
4. الإبقاء على حركة النبض (scale pulse) كما هي
5. الإبقاء على ظل البطاقة

## التعديلات في `src/components/CountdownOverlay.tsx`

### حذف
- الأرقام الكبيرة المائلة في الخلفية (سطور 61-75)
- خلفية `hsla(220 30% 8% / 0.85)` من الحاوية الرئيسية

### تعديل
- الحاوية الرئيسية: `background: transparent` بدون `backdropFilter`
- البطاقة: إزالة `overflow-hidden` لإظهار اللوغو البارز
- اللوغو: `w-40 h-40` مع `-mt-24` وإضافة `absolute` أو `relative` positioning لجعله يطفو فوق حافة البطاقة العلوية
- إضافة `pt-24` للمحتوى الداخلي لتعويض مساحة اللوغو


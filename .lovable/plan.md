

# تعديلات الملف الشخصي

## 1. إضافة عمود `gender` لقاعدة البيانات
- Migration: `ALTER TABLE public.profiles ADD COLUMN gender text NULL;`

## 2. تحديث خيارات "الإطار / المهمة" (`src/lib/i18n.tsx` + `src/pages/Profile.tsx`)

**حذف:** `missionSupportStaff` (إطار الدعم)، `missionEducationalAdvisor` (ملحق تربوي)، `missionGuard` (حارس عام للخارجية)، `missionTreasurer` (قيم على الخزانة)، `missionTechAssistant` (مساعد تقني)، `missionTeacher` (أستاذ(ة))

**إضافة (21 خيار جديد):**
- أستاذ التعليم الإبتدائي / الإعدادي / التأهيلي
- مختص تربوي / إجتماعي / الإدارة والاقتصاد
- متصرف تربوي (مدير / حارس عام للخارجية / للداخلية / ناظر / رئيس الأشغال / مدير الدراسات)
- متصرف القطاعات المشتركة / متصرف وزارة التربية الوطنية
- محرر / مساعد تربوي / تقني
- مفتش تربوي (ابتدائي / إعدادي / تأهيلي) / مفتش التوجيه / التخطيط / المصالح المالية
- طبيب

## 3. حذف بطاقة "السلك" من الملف الشخصي (`src/pages/Profile.tsx`)
- إزالة `corps` من مصفوفة `cards`
- إبقاء `corps` في `form` و `handleSave` لعدم كسر الوظائف الأخرى (auto_assign يعتمد عليه)

## 4. إضافة بطاقة "النوع" (`src/pages/Profile.tsx`)
- حقل select بخيارين: ذكر / أنثى
- إضافة `gender` للـ form state و handleSave

## 5. تعديل تسمية "حالة الانخراط" 
- تغيير `membershipLabel` إلى سطرين: `حالة الانخراط\n(موسم: 26/25)`

## 6. إعادة ترتيب البطاقات
الترتيب الجديد:
1. الاسم الكامل
2. النوع (جديد)
3. رقم التأجير
4. الإطار / المهمة
5. الأكاديمية الجهوية
6. المديرية الإقليمية
7. المؤسسة
8. حالة الانخراط (موسم: 26/25)
9. رقم الهاتف
10. البريد الإلكتروني

## الملفات المعدّلة
- `src/lib/i18n.tsx` — ترجمات جديدة (عربي + فرنسي)
- `src/pages/Profile.tsx` — cards array, form state, gender field, ترتيب جديد
- Migration — إضافة عمود gender

